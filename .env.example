# HSM PIN Configuration
#
# SECURITY NOTICE: This file is a template. DO NOT use these example values in production!
# Copy this file to .env.node1, .env.node2, and .env.node3 with unique strong PINs for each node.
#
# TWO TYPES OF PINS:
# - PIN: User PIN for all cryptographic operations (signing, key usage)
# - SO_PIN: Security Officer PIN for administrative access (reset user PIN, destroy keys)
#
# PIN REQUIREMENTS:
# - Must be 8 digits (range: 10000000-99999999)
# - All 6 PINs must be unique (3 nodes × 2 PINs each)
# - PIN and SO_PIN on the same node must be different
# - Must be cryptographically random (not sequential, not a date, not a phone number)
# - Should be generated using a secure random number generator
#
# GENERATION EXAMPLE (Python):
#   import secrets
#   for i in range(1, 4):
#       pin = secrets.randbelow(90000000) + 10000000
#       so_pin = secrets.randbelow(90000000) + 10000000
#       print(f"Node {i}: PIN={pin}  SO_PIN={so_pin}")
#
# PRODUCTION BEST PRACTICES:
# - Use a secrets management system (HashiCorp Vault, AWS Secrets Manager, etc.)
# - Implement PIN rotation policies
# - Never commit .env.node* files to version control
# - Consider using hardware HSM with stronger authentication in production
# - Audit PIN access and usage regularly
# - Store SO_PIN separately from PIN with different access controls
#
# Each node should have its own .env file:
# - .env.node1 for node1
# - .env.node2 for node2
# - .env.node3 for node3

# HSM User PIN (8 digits, cryptographically random)
PIN=12345678

# HSM Security Officer PIN (8 digits, cryptographically random, different from PIN)
SO_PIN=87654321
